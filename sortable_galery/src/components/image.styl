
.image {
	display: block;
	width: 100%;
	position: relative;
	
	&__image {
		display: block;
		width: 100%;
		z-index: 0;
	}
	
	&__overlay {
		display: block;
		position: absolute;
		top: 0; bottom: 0; left: 0; right: 0;
		z-index: 1;
		background: rgba(255, 255, 255, 0.6);
	}
}
